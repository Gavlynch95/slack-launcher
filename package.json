{
  "name": "slack-launcher",
  "version": "1.0.0",
  "description": "Multi-account Slack launcher with isolated sessions",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "check": "node --check main.js && node --check preload.js && node --check webview-preload.js"
  },
  "devDependencies": {
    "electron": "^43.1.1"
  }
}
